// Placeholder for Microsoft Entra ID (Azure AD) auth via MSAL.
// We will wire this into the app once tenant/app registration details are available.
export async function requireAuth() {
  return { status: "not_configured" };
}

